language: python
python:
  - "3.6"
install:
  - pip install -r requirements.txt --quiet
  - pip install coverage --quiet
script:
  - coverage run -m unittest tests
  - coverage report
  - coverage html